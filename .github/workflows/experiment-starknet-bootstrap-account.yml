name: starknet-bootstrap-account tests
on: workflow_dispatch

permissions:
  contents: read

jobs:
  test:
    uses: ./.github/workflows/_sdks_jest.yml
    with:
      workspace: "starknet-bootstrap-account"
